[build-system]
requires = [
    "setuptools>=62.3.0,<75.9",
    "torch>=2.3.1",
    ]
build-backend = "setuptools.build_meta"

[project]
name = "sam2"
version = "1.0"
requires-python = ">=3.10.0"
dynamic = ["dependencies"]
